import React from 'react';

const FileToFile = () => {                                                            // ES6 syntax for component
    return (
        <div>
            <h1>Hello Dojo</h1>
            <p>This is a demo of passing a React component from file to file</p>
        </div>
    );
}

export default FileToFile;          // make this component available to other files